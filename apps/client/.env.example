# Your Discord Application's OAuth2 Client ID
VITE_DISCORD_CLIENT_ID=

# Optional: Override the Colyseus server URL (defaults to auto-detect via proxy)
# In production, the client is served by the same server, so this is not needed
# VITE_COLYSEUS_URL=ws://localhost:2567
